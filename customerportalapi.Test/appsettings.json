{
  "Logging": {
    "LogLevel": {
      "Default": "Warning"
    }
  },
  "AllowedHosts": "*",
  "ConnectionStrings": {
    "customerportaldb": "${MONGO_URL}"
  },
  "DatabaseName": "${MONGO_DATABASE}",
  "GatewayUrl": "",
  "ProfileAPI": "${PROFILE_API}",
  "ContractsAPI": "${CONTRACTS_API}",
  "StoresAPI": "${SITES_API}",
  "ContractSMAPI": "${CONTRACT_SMAPI}",
  "MailFrom": "${MAIL_FROM}",
  "InviteConfirmation": "${INVITE_URL}signup/",
  "Email": {
    "Smtp": {
      "Host": "${SMTP_HOST}",
      "Port": 587,
      "EnableSSL": false,
      "Username": "${SMTP_USERNAME}",
      "Password": "${SMTP_PASSWORD}"
    }
  },
  "Identity": {
    "Urls": {
      "Authorize": "https://identity-pre.bluespace.eu/oauth2/token",
      "Provisioning": "https://identity-pre.bluespace.eu/wso2/scim/Users"
    },
    "Credential": {
      "ClientId": "j4en6ZOi60uH21VlJD8vzpfp2RAa",
      "ClientSecret": "I5PdMvl9TLEY5Q5sxNNIcxYutfAa"
    }
  },
  "BaseUrl": "${BaseUrl}",
  "MailIT": "${MailIT}",
  "AzureAd": {
    "Instance": "${Instance}",
    "ClientId": "${ClientId}",
    "TenantId": "${TenantId}"
  }
}
